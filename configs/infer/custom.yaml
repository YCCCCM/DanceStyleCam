base_config: default.yaml

experiment:
  name: dsc_custom

input:
  sample_id: custom
  dance: null
  music: null
  pmx: null
  max_frames: null

# Temporal and spatio-temporal controls are optional. Custom input defaults
# to Choreography so the style branch always receives a complete condition.
controls:
  temporal: null
  spatio_temporal: null
  style: Choreography
